{
    "scripts": {
        "frontend": "cd frontend && ./cache-files.sh && npm run dev",
        "backend": "dotnet run --project backend/src/DigitalPassportBackend dev",
        "database": "cd backend/src/DigitalPassportBackend && dotnet ef database update",
        "docker": "./frontend/cache-files.sh && docker compose up --build",
        "docker:clear": "docker system prune"
    },
    "devDependencies": {
        "@biomejs/biome": "1.9.4"
    }
}
